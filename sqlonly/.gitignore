# ── 올리면 안 되는 것 ─────────────────────────────────────────
# 용량이 크거나, 각자 컴퓨터에서 다시 만들면 되는 것들

# 원본 데이터 (131MB — GitHub에 올리면 안 됩니다)
data/okcupid_profiles.csv
data/okcupid_cleaned*.csv
data/*.db

# 모델 파일 (모델 담당만 가지고 있으면 됩니다)
models/*.cbm

# 파이썬 찌꺼기
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/

# 에디터
.vscode/
.idea/
.DS_Store

# ── 반드시 올려야 하는 것 ─────────────────────────────────────
# sql/init/predictions.csv (21MB)
#   이게 없으면 팀원이 docker compose up -d 해도 테이블이 비어 있습니다.
#   .gitignore 에 넣지 마세요.
!sql/init/predictions.csv
